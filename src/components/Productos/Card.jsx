import React from 'react';

const Card = ({ title, imageUrl, description, link }) => {
  return (
    <div className="w-[300px] bg-five">
      <h1>AAAAAA</h1>
      <h2>{title}</h2>
      <p>{imageUrl}</p>
      <p>{description}</p>
    </div>
  );
};

export default Card;
